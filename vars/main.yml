---
supported_arch:
  x86_64: amd64
supported_os:
  - Debian
  - RedHat
go_storage_url: "https://storage.googleapis.com/golang/"
current_arch: "{{ hostvars[inventory_hostname].ansible_architecture }}"
go_version: latest
